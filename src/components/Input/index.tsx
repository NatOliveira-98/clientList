import { Container } from './styles';

export const Input = () => {
  return (
    <Container>
      <input type="text" placeholder="Procure pelo nome ou tipo de serviço" />
    </Container>
  );
};
